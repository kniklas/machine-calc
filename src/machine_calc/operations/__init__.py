"""Operation registry/dispatch (future: turning, milling register alongside
drilling here per Constitution Principle VI). Currently empty — drilling is
the only implemented operation and is imported directly by
``machine_calc/__init__.py``.
"""
